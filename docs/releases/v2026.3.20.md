# v2026.3.20 — 2026-03-02

## Fixed
- **list_plans** — require controller handshake before `read_all_plan`; accept data_feedback when payload contains plan list even if `topic` differs; parse nested `data.data` and `id`/`name` (firmware compatibility)
- **list_schedules** — same: controller, `accept_if` for schedule-list payloads, `scheduleList`/`schedules`/`data` extraction
- **get_global_params**, **get_map** — call `_ensure_controller()` before publish so robot responds
- **YarboPlan.from_dict** / **YarboSchedule.from_dict** — coerce numeric `id` to string for `plan_id`/`schedule_id`

## Added
- **mqtt.wait_for_message** — optional `accept_if` predicate to accept responses by payload shape when firmware echoes a different topic
